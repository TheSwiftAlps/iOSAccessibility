import UIKit

enum Constants {
    struct Images {
        static let berryTart = UIImage(named: "berryTart")!
        static let lemonTart = UIImage(named: "lemonTart")!
        static let chocolateTart = UIImage(named: "chocolateTart")!
        static let chevron = UIImage(named: "chevron")!
    }
}
